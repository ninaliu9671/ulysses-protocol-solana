/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findCommitmentPda, findRewardPoolPda, findVaultPda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CREATE_AGENT_GUARDIAN_DISCRIMINATOR = new Uint8Array([
  22, 83, 245, 78, 152, 246, 63, 70,
]);

export function getCreateAgentGuardianDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_AGENT_GUARDIAN_DISCRIMINATOR,
  );
}

export type CreateAgentGuardianInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountCommitment extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountRewardPool extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountCommitment extends string
        ? WritableAccount<TAccountCommitment>
        : TAccountCommitment,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountRewardPool extends string
        ? WritableAccount<TAccountRewardPool>
        : TAccountRewardPool,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateAgentGuardianInstructionData = {
  discriminator: ReadonlyUint8Array;
  stakeAmount: bigint;
  durationSeconds: bigint;
  guardianPubkey: Address;
};

export type CreateAgentGuardianInstructionDataArgs = {
  stakeAmount: number | bigint;
  durationSeconds: number | bigint;
  guardianPubkey: Address;
};

export function getCreateAgentGuardianInstructionDataEncoder(): FixedSizeEncoder<CreateAgentGuardianInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["stakeAmount", getU64Encoder()],
      ["durationSeconds", getU64Encoder()],
      ["guardianPubkey", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: CREATE_AGENT_GUARDIAN_DISCRIMINATOR,
    }),
  );
}

export function getCreateAgentGuardianInstructionDataDecoder(): FixedSizeDecoder<CreateAgentGuardianInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["stakeAmount", getU64Decoder()],
    ["durationSeconds", getU64Decoder()],
    ["guardianPubkey", getAddressDecoder()],
  ]);
}

export function getCreateAgentGuardianInstructionDataCodec(): FixedSizeCodec<
  CreateAgentGuardianInstructionDataArgs,
  CreateAgentGuardianInstructionData
> {
  return combineCodec(
    getCreateAgentGuardianInstructionDataEncoder(),
    getCreateAgentGuardianInstructionDataDecoder(),
  );
}

export type CreateAgentGuardianAsyncInput<
  TAccountOwner extends string = string,
  TAccountCommitment extends string = string,
  TAccountVault extends string = string,
  TAccountRewardPool extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  commitment?: Address<TAccountCommitment>;
  vault?: Address<TAccountVault>;
  rewardPool?: Address<TAccountRewardPool>;
  systemProgram?: Address<TAccountSystemProgram>;
  stakeAmount: CreateAgentGuardianInstructionDataArgs["stakeAmount"];
  durationSeconds: CreateAgentGuardianInstructionDataArgs["durationSeconds"];
  guardianPubkey: CreateAgentGuardianInstructionDataArgs["guardianPubkey"];
};

export async function getCreateAgentGuardianInstructionAsync<
  TAccountOwner extends string,
  TAccountCommitment extends string,
  TAccountVault extends string,
  TAccountRewardPool extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: CreateAgentGuardianAsyncInput<
    TAccountOwner,
    TAccountCommitment,
    TAccountVault,
    TAccountRewardPool,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateAgentGuardianInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountCommitment,
    TAccountVault,
    TAccountRewardPool,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    commitment: { value: input.commitment ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    rewardPool: { value: input.rewardPool ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.commitment.value) {
    accounts.commitment.value = await findCommitmentPda({
      owner: expectAddress(accounts.owner.value),
    });
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda({
      commitment: expectAddress(accounts.commitment.value),
    });
  }
  if (!accounts.rewardPool.value) {
    accounts.rewardPool.value = await findRewardPoolPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.commitment),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.rewardPool),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateAgentGuardianInstructionDataEncoder().encode(
      args as CreateAgentGuardianInstructionDataArgs,
    ),
    programAddress,
  } as CreateAgentGuardianInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountCommitment,
    TAccountVault,
    TAccountRewardPool,
    TAccountSystemProgram
  >);
}

export type CreateAgentGuardianInput<
  TAccountOwner extends string = string,
  TAccountCommitment extends string = string,
  TAccountVault extends string = string,
  TAccountRewardPool extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  commitment: Address<TAccountCommitment>;
  vault: Address<TAccountVault>;
  rewardPool: Address<TAccountRewardPool>;
  systemProgram?: Address<TAccountSystemProgram>;
  stakeAmount: CreateAgentGuardianInstructionDataArgs["stakeAmount"];
  durationSeconds: CreateAgentGuardianInstructionDataArgs["durationSeconds"];
  guardianPubkey: CreateAgentGuardianInstructionDataArgs["guardianPubkey"];
};

export function getCreateAgentGuardianInstruction<
  TAccountOwner extends string,
  TAccountCommitment extends string,
  TAccountVault extends string,
  TAccountRewardPool extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: CreateAgentGuardianInput<
    TAccountOwner,
    TAccountCommitment,
    TAccountVault,
    TAccountRewardPool,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateAgentGuardianInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountCommitment,
  TAccountVault,
  TAccountRewardPool,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    commitment: { value: input.commitment ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    rewardPool: { value: input.rewardPool ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.commitment),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.rewardPool),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateAgentGuardianInstructionDataEncoder().encode(
      args as CreateAgentGuardianInstructionDataArgs,
    ),
    programAddress,
  } as CreateAgentGuardianInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountCommitment,
    TAccountVault,
    TAccountRewardPool,
    TAccountSystemProgram
  >);
}

export type ParsedCreateAgentGuardianInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    commitment: TAccountMetas[1];
    vault: TAccountMetas[2];
    rewardPool: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: CreateAgentGuardianInstructionData;
};

export function parseCreateAgentGuardianInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateAgentGuardianInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      commitment: getNextAccount(),
      vault: getNextAccount(),
      rewardPool: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateAgentGuardianInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
