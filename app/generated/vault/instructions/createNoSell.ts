/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  findCommitmentPda,
  findCreateHoldAboveCommitmentPda,
  findNoSellPdaPda,
  findRewardPoolPda,
  findVaultPda,
} from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CREATE_NO_SELL_DISCRIMINATOR = new Uint8Array([
  159, 86, 62, 92, 65, 223, 161, 14,
]);

export function getCreateNoSellDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_NO_SELL_DISCRIMINATOR,
  );
}

export type CreateNoSellInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountTargetMint extends string | AccountMeta<string> = string,
  TAccountCommitment extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountRewardPool extends string | AccountMeta<string> = string,
  TAccountHoldAbovePda extends string | AccountMeta<string> = string,
  TAccountAgentGuardianPda extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountTargetMint extends string
        ? ReadonlyAccount<TAccountTargetMint>
        : TAccountTargetMint,
      TAccountCommitment extends string
        ? WritableAccount<TAccountCommitment>
        : TAccountCommitment,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountRewardPool extends string
        ? WritableAccount<TAccountRewardPool>
        : TAccountRewardPool,
      TAccountHoldAbovePda extends string
        ? ReadonlyAccount<TAccountHoldAbovePda>
        : TAccountHoldAbovePda,
      TAccountAgentGuardianPda extends string
        ? ReadonlyAccount<TAccountAgentGuardianPda>
        : TAccountAgentGuardianPda,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateNoSellInstructionData = {
  discriminator: ReadonlyUint8Array;
  stakeAmount: bigint;
  durationSeconds: bigint;
};

export type CreateNoSellInstructionDataArgs = {
  stakeAmount: number | bigint;
  durationSeconds: number | bigint;
};

export function getCreateNoSellInstructionDataEncoder(): FixedSizeEncoder<CreateNoSellInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["stakeAmount", getU64Encoder()],
      ["durationSeconds", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_NO_SELL_DISCRIMINATOR }),
  );
}

export function getCreateNoSellInstructionDataDecoder(): FixedSizeDecoder<CreateNoSellInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["stakeAmount", getU64Decoder()],
    ["durationSeconds", getU64Decoder()],
  ]);
}

export function getCreateNoSellInstructionDataCodec(): FixedSizeCodec<
  CreateNoSellInstructionDataArgs,
  CreateNoSellInstructionData
> {
  return combineCodec(
    getCreateNoSellInstructionDataEncoder(),
    getCreateNoSellInstructionDataDecoder(),
  );
}

export type CreateNoSellAsyncInput<
  TAccountOwner extends string = string,
  TAccountTargetMint extends string = string,
  TAccountCommitment extends string = string,
  TAccountVault extends string = string,
  TAccountRewardPool extends string = string,
  TAccountHoldAbovePda extends string = string,
  TAccountAgentGuardianPda extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  targetMint: Address<TAccountTargetMint>;
  commitment?: Address<TAccountCommitment>;
  vault?: Address<TAccountVault>;
  rewardPool?: Address<TAccountRewardPool>;
  holdAbovePda?: Address<TAccountHoldAbovePda>;
  agentGuardianPda?: Address<TAccountAgentGuardianPda>;
  systemProgram?: Address<TAccountSystemProgram>;
  stakeAmount: CreateNoSellInstructionDataArgs["stakeAmount"];
  durationSeconds: CreateNoSellInstructionDataArgs["durationSeconds"];
};

export async function getCreateNoSellInstructionAsync<
  TAccountOwner extends string,
  TAccountTargetMint extends string,
  TAccountCommitment extends string,
  TAccountVault extends string,
  TAccountRewardPool extends string,
  TAccountHoldAbovePda extends string,
  TAccountAgentGuardianPda extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: CreateNoSellAsyncInput<
    TAccountOwner,
    TAccountTargetMint,
    TAccountCommitment,
    TAccountVault,
    TAccountRewardPool,
    TAccountHoldAbovePda,
    TAccountAgentGuardianPda,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateNoSellInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountTargetMint,
    TAccountCommitment,
    TAccountVault,
    TAccountRewardPool,
    TAccountHoldAbovePda,
    TAccountAgentGuardianPda,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    targetMint: { value: input.targetMint ?? null, isWritable: false },
    commitment: { value: input.commitment ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    rewardPool: { value: input.rewardPool ?? null, isWritable: true },
    holdAbovePda: { value: input.holdAbovePda ?? null, isWritable: false },
    agentGuardianPda: {
      value: input.agentGuardianPda ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.commitment.value) {
    accounts.commitment.value = await findNoSellPdaPda({
      owner: expectAddress(accounts.owner.value),
      targetMint: expectAddress(accounts.targetMint.value),
    });
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda({
      commitment: expectAddress(accounts.commitment.value),
    });
  }
  if (!accounts.rewardPool.value) {
    accounts.rewardPool.value = await findRewardPoolPda();
  }
  if (!accounts.holdAbovePda.value) {
    accounts.holdAbovePda.value = await findCreateHoldAboveCommitmentPda({
      owner: expectAddress(accounts.owner.value),
      targetMint: expectAddress(accounts.targetMint.value),
    });
  }
  if (!accounts.agentGuardianPda.value) {
    accounts.agentGuardianPda.value = await findCommitmentPda({
      owner: expectAddress(accounts.owner.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.targetMint),
      getAccountMeta(accounts.commitment),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.rewardPool),
      getAccountMeta(accounts.holdAbovePda),
      getAccountMeta(accounts.agentGuardianPda),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateNoSellInstructionDataEncoder().encode(
      args as CreateNoSellInstructionDataArgs,
    ),
    programAddress,
  } as CreateNoSellInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountTargetMint,
    TAccountCommitment,
    TAccountVault,
    TAccountRewardPool,
    TAccountHoldAbovePda,
    TAccountAgentGuardianPda,
    TAccountSystemProgram
  >);
}

export type CreateNoSellInput<
  TAccountOwner extends string = string,
  TAccountTargetMint extends string = string,
  TAccountCommitment extends string = string,
  TAccountVault extends string = string,
  TAccountRewardPool extends string = string,
  TAccountHoldAbovePda extends string = string,
  TAccountAgentGuardianPda extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  targetMint: Address<TAccountTargetMint>;
  commitment: Address<TAccountCommitment>;
  vault: Address<TAccountVault>;
  rewardPool: Address<TAccountRewardPool>;
  holdAbovePda: Address<TAccountHoldAbovePda>;
  agentGuardianPda: Address<TAccountAgentGuardianPda>;
  systemProgram?: Address<TAccountSystemProgram>;
  stakeAmount: CreateNoSellInstructionDataArgs["stakeAmount"];
  durationSeconds: CreateNoSellInstructionDataArgs["durationSeconds"];
};

export function getCreateNoSellInstruction<
  TAccountOwner extends string,
  TAccountTargetMint extends string,
  TAccountCommitment extends string,
  TAccountVault extends string,
  TAccountRewardPool extends string,
  TAccountHoldAbovePda extends string,
  TAccountAgentGuardianPda extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: CreateNoSellInput<
    TAccountOwner,
    TAccountTargetMint,
    TAccountCommitment,
    TAccountVault,
    TAccountRewardPool,
    TAccountHoldAbovePda,
    TAccountAgentGuardianPda,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateNoSellInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountTargetMint,
  TAccountCommitment,
  TAccountVault,
  TAccountRewardPool,
  TAccountHoldAbovePda,
  TAccountAgentGuardianPda,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    targetMint: { value: input.targetMint ?? null, isWritable: false },
    commitment: { value: input.commitment ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    rewardPool: { value: input.rewardPool ?? null, isWritable: true },
    holdAbovePda: { value: input.holdAbovePda ?? null, isWritable: false },
    agentGuardianPda: {
      value: input.agentGuardianPda ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.targetMint),
      getAccountMeta(accounts.commitment),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.rewardPool),
      getAccountMeta(accounts.holdAbovePda),
      getAccountMeta(accounts.agentGuardianPda),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateNoSellInstructionDataEncoder().encode(
      args as CreateNoSellInstructionDataArgs,
    ),
    programAddress,
  } as CreateNoSellInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountTargetMint,
    TAccountCommitment,
    TAccountVault,
    TAccountRewardPool,
    TAccountHoldAbovePda,
    TAccountAgentGuardianPda,
    TAccountSystemProgram
  >);
}

export type ParsedCreateNoSellInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    targetMint: TAccountMetas[1];
    commitment: TAccountMetas[2];
    vault: TAccountMetas[3];
    rewardPool: TAccountMetas[4];
    holdAbovePda: TAccountMetas[5];
    agentGuardianPda: TAccountMetas[6];
    systemProgram: TAccountMetas[7];
  };
  data: CreateNoSellInstructionData;
};

export function parseCreateNoSellInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateNoSellInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      targetMint: getNextAccount(),
      commitment: getNextAccount(),
      vault: getNextAccount(),
      rewardPool: getNextAccount(),
      holdAbovePda: getNextAccount(),
      agentGuardianPda: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateNoSellInstructionDataDecoder().decode(instruction.data),
  };
}
