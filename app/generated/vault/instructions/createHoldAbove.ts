/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  findCommitmentPda,
  findCreateHoldAboveCommitmentPda,
  findNoSellPdaPda,
  findRewardPoolPda,
  findVaultPda,
} from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CREATE_HOLD_ABOVE_DISCRIMINATOR = new Uint8Array([
  235, 155, 148, 63, 69, 58, 49, 33,
]);

export function getCreateHoldAboveDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_HOLD_ABOVE_DISCRIMINATOR,
  );
}

export type CreateHoldAboveInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountTargetMint extends string | AccountMeta<string> = string,
  TAccountCommitment extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountRewardPool extends string | AccountMeta<string> = string,
  TAccountNoSellPda extends string | AccountMeta<string> = string,
  TAccountAgentGuardianPda extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountTargetMint extends string
        ? ReadonlyAccount<TAccountTargetMint>
        : TAccountTargetMint,
      TAccountCommitment extends string
        ? WritableAccount<TAccountCommitment>
        : TAccountCommitment,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountRewardPool extends string
        ? WritableAccount<TAccountRewardPool>
        : TAccountRewardPool,
      TAccountNoSellPda extends string
        ? ReadonlyAccount<TAccountNoSellPda>
        : TAccountNoSellPda,
      TAccountAgentGuardianPda extends string
        ? ReadonlyAccount<TAccountAgentGuardianPda>
        : TAccountAgentGuardianPda,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateHoldAboveInstructionData = {
  discriminator: ReadonlyUint8Array;
  stakeAmount: bigint;
  durationDays: number;
  floorAmount: bigint;
};

export type CreateHoldAboveInstructionDataArgs = {
  stakeAmount: number | bigint;
  durationDays: number;
  floorAmount: number | bigint;
};

export function getCreateHoldAboveInstructionDataEncoder(): FixedSizeEncoder<CreateHoldAboveInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["stakeAmount", getU64Encoder()],
      ["durationDays", getU16Encoder()],
      ["floorAmount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_HOLD_ABOVE_DISCRIMINATOR }),
  );
}

export function getCreateHoldAboveInstructionDataDecoder(): FixedSizeDecoder<CreateHoldAboveInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["stakeAmount", getU64Decoder()],
    ["durationDays", getU16Decoder()],
    ["floorAmount", getU64Decoder()],
  ]);
}

export function getCreateHoldAboveInstructionDataCodec(): FixedSizeCodec<
  CreateHoldAboveInstructionDataArgs,
  CreateHoldAboveInstructionData
> {
  return combineCodec(
    getCreateHoldAboveInstructionDataEncoder(),
    getCreateHoldAboveInstructionDataDecoder(),
  );
}

export type CreateHoldAboveAsyncInput<
  TAccountOwner extends string = string,
  TAccountTargetMint extends string = string,
  TAccountCommitment extends string = string,
  TAccountVault extends string = string,
  TAccountRewardPool extends string = string,
  TAccountNoSellPda extends string = string,
  TAccountAgentGuardianPda extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  targetMint: Address<TAccountTargetMint>;
  commitment?: Address<TAccountCommitment>;
  vault?: Address<TAccountVault>;
  rewardPool?: Address<TAccountRewardPool>;
  noSellPda?: Address<TAccountNoSellPda>;
  agentGuardianPda?: Address<TAccountAgentGuardianPda>;
  systemProgram?: Address<TAccountSystemProgram>;
  stakeAmount: CreateHoldAboveInstructionDataArgs["stakeAmount"];
  durationDays: CreateHoldAboveInstructionDataArgs["durationDays"];
  floorAmount: CreateHoldAboveInstructionDataArgs["floorAmount"];
};

export async function getCreateHoldAboveInstructionAsync<
  TAccountOwner extends string,
  TAccountTargetMint extends string,
  TAccountCommitment extends string,
  TAccountVault extends string,
  TAccountRewardPool extends string,
  TAccountNoSellPda extends string,
  TAccountAgentGuardianPda extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: CreateHoldAboveAsyncInput<
    TAccountOwner,
    TAccountTargetMint,
    TAccountCommitment,
    TAccountVault,
    TAccountRewardPool,
    TAccountNoSellPda,
    TAccountAgentGuardianPda,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateHoldAboveInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountTargetMint,
    TAccountCommitment,
    TAccountVault,
    TAccountRewardPool,
    TAccountNoSellPda,
    TAccountAgentGuardianPda,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    targetMint: { value: input.targetMint ?? null, isWritable: false },
    commitment: { value: input.commitment ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    rewardPool: { value: input.rewardPool ?? null, isWritable: true },
    noSellPda: { value: input.noSellPda ?? null, isWritable: false },
    agentGuardianPda: {
      value: input.agentGuardianPda ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.commitment.value) {
    accounts.commitment.value = await findCreateHoldAboveCommitmentPda({
      owner: expectAddress(accounts.owner.value),
      targetMint: expectAddress(accounts.targetMint.value),
    });
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda({
      commitment: expectAddress(accounts.commitment.value),
    });
  }
  if (!accounts.rewardPool.value) {
    accounts.rewardPool.value = await findRewardPoolPda();
  }
  if (!accounts.noSellPda.value) {
    accounts.noSellPda.value = await findNoSellPdaPda({
      owner: expectAddress(accounts.owner.value),
      targetMint: expectAddress(accounts.targetMint.value),
    });
  }
  if (!accounts.agentGuardianPda.value) {
    accounts.agentGuardianPda.value = await findCommitmentPda({
      owner: expectAddress(accounts.owner.value),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.targetMint),
      getAccountMeta(accounts.commitment),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.rewardPool),
      getAccountMeta(accounts.noSellPda),
      getAccountMeta(accounts.agentGuardianPda),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateHoldAboveInstructionDataEncoder().encode(
      args as CreateHoldAboveInstructionDataArgs,
    ),
    programAddress,
  } as CreateHoldAboveInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountTargetMint,
    TAccountCommitment,
    TAccountVault,
    TAccountRewardPool,
    TAccountNoSellPda,
    TAccountAgentGuardianPda,
    TAccountSystemProgram
  >);
}

export type CreateHoldAboveInput<
  TAccountOwner extends string = string,
  TAccountTargetMint extends string = string,
  TAccountCommitment extends string = string,
  TAccountVault extends string = string,
  TAccountRewardPool extends string = string,
  TAccountNoSellPda extends string = string,
  TAccountAgentGuardianPda extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  targetMint: Address<TAccountTargetMint>;
  commitment: Address<TAccountCommitment>;
  vault: Address<TAccountVault>;
  rewardPool: Address<TAccountRewardPool>;
  noSellPda: Address<TAccountNoSellPda>;
  agentGuardianPda: Address<TAccountAgentGuardianPda>;
  systemProgram?: Address<TAccountSystemProgram>;
  stakeAmount: CreateHoldAboveInstructionDataArgs["stakeAmount"];
  durationDays: CreateHoldAboveInstructionDataArgs["durationDays"];
  floorAmount: CreateHoldAboveInstructionDataArgs["floorAmount"];
};

export function getCreateHoldAboveInstruction<
  TAccountOwner extends string,
  TAccountTargetMint extends string,
  TAccountCommitment extends string,
  TAccountVault extends string,
  TAccountRewardPool extends string,
  TAccountNoSellPda extends string,
  TAccountAgentGuardianPda extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: CreateHoldAboveInput<
    TAccountOwner,
    TAccountTargetMint,
    TAccountCommitment,
    TAccountVault,
    TAccountRewardPool,
    TAccountNoSellPda,
    TAccountAgentGuardianPda,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateHoldAboveInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountTargetMint,
  TAccountCommitment,
  TAccountVault,
  TAccountRewardPool,
  TAccountNoSellPda,
  TAccountAgentGuardianPda,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    targetMint: { value: input.targetMint ?? null, isWritable: false },
    commitment: { value: input.commitment ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    rewardPool: { value: input.rewardPool ?? null, isWritable: true },
    noSellPda: { value: input.noSellPda ?? null, isWritable: false },
    agentGuardianPda: {
      value: input.agentGuardianPda ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.targetMint),
      getAccountMeta(accounts.commitment),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.rewardPool),
      getAccountMeta(accounts.noSellPda),
      getAccountMeta(accounts.agentGuardianPda),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateHoldAboveInstructionDataEncoder().encode(
      args as CreateHoldAboveInstructionDataArgs,
    ),
    programAddress,
  } as CreateHoldAboveInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountTargetMint,
    TAccountCommitment,
    TAccountVault,
    TAccountRewardPool,
    TAccountNoSellPda,
    TAccountAgentGuardianPda,
    TAccountSystemProgram
  >);
}

export type ParsedCreateHoldAboveInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    targetMint: TAccountMetas[1];
    commitment: TAccountMetas[2];
    vault: TAccountMetas[3];
    rewardPool: TAccountMetas[4];
    noSellPda: TAccountMetas[5];
    agentGuardianPda: TAccountMetas[6];
    systemProgram: TAccountMetas[7];
  };
  data: CreateHoldAboveInstructionData;
};

export function parseCreateHoldAboveInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateHoldAboveInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      targetMint: getNextAccount(),
      commitment: getNextAccount(),
      vault: getNextAccount(),
      rewardPool: getNextAccount(),
      noSellPda: getNextAccount(),
      agentGuardianPda: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateHoldAboveInstructionDataDecoder().decode(instruction.data),
  };
}
